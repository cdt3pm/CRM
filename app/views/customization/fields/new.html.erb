<html>
	<head>
	</head>
	<body>
		<h3>New Entity</h3>
		<%= form_for [:customization, @field] do |f| %>
			<%= f.label :name %>
			<%= f.text_field :name %>
			
			<%= f.label :display_name %>
			<%= f.text_field :display_name %>

			<%= f.label :type %>
			<%= f.text_field :type %>

			<%= f.hidden_field :entity %>
			<%= f.submit %>
		<% end %>
	</body>
</html>
